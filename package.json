{
  "name": "eu-store-guard",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "apps/worker"
  ],
  "scripts": {
    "lint": "eslint . && node scripts/secret-scan.mjs",
    "test": "npm test --workspaces",
    "build": "npm run build --workspace apps/worker",
    "deploy:dry-run": "wrangler deploy --dry-run --cwd apps/worker",
    "audit:ci": "npm audit --audit-level=high",
    "bundle:check": "node scripts/bundle-check.mjs",
    "bundle:build": "node scripts/bundle-rules.mjs"
  },
  "devDependencies": {
    "eslint": "9.15.0",
    "@eslint/js": "9.15.0",
    "globals": "15.12.0",
    "wrangler": "4.129.0"
  },
  "engines": {
    "node": ">=20"
  }
}
