{
  "name": "@eu-store-guard/worker",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node ../../scripts/build-check.mjs && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@eu-store-guard/core": "*",
    "saxes": "6.0.0"
  },
  "devDependencies": {
    "liquidjs": "10.29.0"
  }
}
